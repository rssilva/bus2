machine:
  ruby:
      version:
        2.2.1-p85
deployment:
  staging:
    branch: master
    commands:
      # - gem install bundler
      - git fetch origin --unshallow
      - RAILS_ENV=test rake db:create
      - RAILS_ENV=test rake db:migrate
      - rspec
      - ssh rssilva@159.8.108.186 "echo test > /log"
      # - ssh rssilva@159.8.108.186 "bundle install"
      # - ssh rssilva@159.8.108.186 "bundle install"
      # - ssh rssilva@159.8.108.186 "RAILS_ENV=production rake db:create"
      # - ssh rssilva@159.8.108.186 "RAILS_ENV=production rake db:migrate"
      # - ssh rssilva@159.8.108.186 "RAILS_ENV=production rails s Puma -p 80"
